import React from 'react';

// Minimal Toaster stub so the app can run without the original shadcn/ui dependency.
// The project already uses `react-hot-toast` in some places.
export function Toaster() {
  return null;
}

